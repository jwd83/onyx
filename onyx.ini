# Onyx builds its own website (https://onyx.jwd.me) from the docs/ vault below —
# Onyx is, itself, an Onyx site. Keep `source = docs` so the plans/ folder
# (internal architecture reviews) and the onyx/ Go source stay out of the build.
site_title = Onyx
source = docs
theme = theme
search = true
graph = true
show_source = true
